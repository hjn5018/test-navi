# 🏗️ Test-Navi 개발용 AWS 서비스 구성 평가 및 자원 명세

## 산정 공통 조건

| 항목 | 내용 |
|------|------|
| **리전** | 아시아 태평양(서울) `ap-northeast-2` |
| **운영체제** | **Windows** (Playwright headless 테스트 필요) |
| **Free Tier** | **미적용** (프리티어 종료 후 기준) |
| **가동 시간** | 하루 **8시간** × 월 30일 = **월 240시간** (EC2 기준) |
| **환율** | 1 USD = **1,486 KRW** |

> [!WARNING]
> - **EBS 볼륨**: 인스턴스 중지 중에도 프로비저닝된 용량 기준으로 **24시간 과금** (볼륨 삭제 전까지)
> - **탄력적 IP**: 인스턴스 중지 여부와 무관하게 할당된 순간부터 **24시간 과금** (해제 전까지)

---

## 시나리오 A — 외부 LLM API 사용 (t3.small)

> 백엔드에서 GPT-4o / Gemini / Claude 등 외부 API를 호출하고, Playwright headless 브라우저 테스트를 Windows 환경에서 수행하는 구성

### A. 구성 평가

| 항목 | 사양 | 평가 |
|------|------|------|
| **인스턴스** | t3.small (2 vCPU, 2GiB RAM) | ✅ FastAPI + Playwright headless + 외부 API 호출 방식에 적합 |
| **스토리지** | gp3 25GiB | ✅ OS(~15GiB) + Python 환경(~3GiB) + 프로젝트(~5GiB) + 여유 |
| **GPU** | 없음 | ✅ 외부 LLM API 사용 시 GPU 불필요 |
| **네트워크** | 최대 5Gbps | ✅ 개발 단계 충분 |

### A. 자원 명세서

| 순번 | 실습재료명 | 규격 | 단위 | 수량 | 단가 (USD) | 총계 (USD) | URL |
|:----:|-----------|------|:----:|:----:|-----:|-----:|-----|
| 1 | Amazon EC2 인스턴스 | t3.small (2 vCPU, 2GiB RAM) / **Windows** On-Demand | 시간 | 240 | $0.0444 | **$10.66** | [EC2 요금](https://aws.amazon.com/ko/ec2/pricing/on-demand/) |
| 2 | Amazon EBS 볼륨 | 범용 SSD (gp3) 25GiB — 상시 과금 | GB/월 | 25 | $0.0912 | **$2.28** | [EBS 요금](https://aws.amazon.com/ko/ebs/pricing/) |
| 3 | 퍼블릭 IPv4 주소 | 탄력적 IP (Elastic IP) — 24시간 과금 | 시간 | 720 | $0.0050 | **$3.60** | [VPC 요금](https://aws.amazon.com/ko/vpc/pricing/) |
| 4 | Amazon VPC | 기본 VPC + 서브넷 + 인터넷 게이트웨이 | 식 | 1 | $0.00 | **$0.00** | [VPC 요금](https://aws.amazon.com/ko/vpc/pricing/) |
| 5 | 보안 그룹 | SSH/RDP, HTTP(80), HTTPS(443), FastAPI(8000) | 식 | 1 | $0.00 | **$0.00** | [VPC 요금](https://aws.amazon.com/ko/vpc/pricing/) |

### A. 월간 비용 요약

| 항목 | 산정 근거 | 월 비용 (USD) | 월 비용 (KRW) |
|------|----------|-------------:|-------------------:|
| EC2 (t3.small Windows) | 240h × $0.0444/hr | $10.66 | ₩15,841 |
| EBS (gp3, 25GiB) | 25GB × $0.0912/GB/월 | $2.28 | ₩3,388 |
| 퍼블릭 IPv4 (탄력적 IP) | 720h × $0.005/hr | $3.60 | ₩5,350 |
| VPC / 보안 그룹 | — | $0.00 | ₩0 |
| **합계** | | **$16.54** | **₩24,579** |

---

## 시나리오 B — 로컬 LLM 추론 (g4dn.xlarge)

> 7B~13B 파라미터 모델(Gemma, Llama 등)을 EC2에서 직접 추론하면서 Playwright headless 브라우저 테스트도 함께 수행하는 구성

### B. 구성 평가

| 항목 | 사양 | 평가 |
|------|------|------|
| **인스턴스** | g4dn.xlarge (4 vCPU, 16GiB RAM) | ✅ FastAPI + Playwright + 로컬 LLM 추론 동시 운영 가능 |
| **GPU** | NVIDIA T4 16GB VRAM | ✅ 7B 양자화 모델 추론 가능 (GGUF Q4: ~4GB VRAM) |
| **로컬 스토리지** | 125GB NVMe SSD (인스턴스 스토리지) | ✅ 임시 데이터 / 캐시용 (인스턴스 중지 시 데이터 소멸) |
| **EBS 스토리지** | gp3 50GiB | ✅ OS(~15GiB) + 환경(~5GiB) + 모델 가중치(~10GiB) + 프로젝트(~5GiB) + 여유 |
| **네트워크** | 최대 25Gbps | ✅ 충분 |

> [!NOTE]
> **로컬 추론 가능 모델 예시 (NVIDIA T4 16GB VRAM 기준)**
> | 모델 | 양자화 | VRAM 사용량 | 추론 속도 |
> |------|--------|-----------|----------|
> | Gemma 2B (GGUF Q4) | 4-bit | ~1.5GB | 빠름 |
> | Llama 3 8B (GGUF Q4) | 4-bit | ~4.5GB | 적당 |
> | Gemma 7B (GGUF Q4) | 4-bit | ~4GB | 적당 |
> | Llama 3 13B (GGUF Q4) | 4-bit | ~7.5GB | 다소 느림 |
> | Llama 3 70B | — | ❌ VRAM 초과 | 불가 |

### B. 자원 명세서

| 순번 | 실습재료명 | 규격 | 단위 | 수량 | 단가 (USD) | 총계 (USD) | URL |
|:----:|-----------|------|:----:|:----:|-----:|-----:|-----|
| 1 | Amazon EC2 인스턴스 | g4dn.xlarge (4 vCPU, 16GiB RAM, NVIDIA T4 GPU) / **Windows** On-Demand | 시간 | 240 | $0.8310 | **$199.44** | [EC2 요금](https://aws.amazon.com/ko/ec2/pricing/on-demand/) |
| 2 | Amazon EBS 볼륨 | 범용 SSD (gp3) 50GiB — 상시 과금 | GB/월 | 50 | $0.0912 | **$4.56** | [EBS 요금](https://aws.amazon.com/ko/ebs/pricing/) |
| 3 | 퍼블릭 IPv4 주소 | 탄력적 IP (Elastic IP) — 24시간 과금 | 시간 | 720 | $0.0050 | **$3.60** | [VPC 요금](https://aws.amazon.com/ko/vpc/pricing/) |
| 4 | Amazon VPC | 기본 VPC + 서브넷 + 인터넷 게이트웨이 | 식 | 1 | $0.00 | **$0.00** | [VPC 요금](https://aws.amazon.com/ko/vpc/pricing/) |
| 5 | 보안 그룹 | RDP(3389), HTTP(80), HTTPS(443), FastAPI(8000) | 식 | 1 | $0.00 | **$0.00** | [VPC 요금](https://aws.amazon.com/ko/vpc/pricing/) |

### B. 월간 비용 요약

| 항목 | 산정 근거 | 월 비용 (USD) | 월 비용 (KRW) |
|------|----------|-------------:|-------------------:|
| EC2 (g4dn.xlarge Windows) | 240h × $0.831/hr | $199.44 | ₩296,368 |
| EBS (gp3, 50GiB) | 50GB × $0.0912/GB/월 | $4.56 | ₩6,776 |
| 퍼블릭 IPv4 (탄력적 IP) | 720h × $0.005/hr | $3.60 | ₩5,350 |
| VPC / 보안 그룹 | — | $0.00 | ₩0 |
| **합계** | | **$207.60** | **₩308,494** |

---

## 시나리오 비교

| 비교 항목 | 시나리오 A (t3.small) | 시나리오 B (g4dn.xlarge) |
|----------|:--------------------:|:-----------------------:|
| **월 비용 (USD)** | **$16.54** | **$207.60** |
| **월 비용 (KRW)** | **₩24,579** | **₩308,494** |
| **비용 배수** | 1× (기준) | **12.6×** |
| **vCPU** | 2 | 4 |
| **RAM** | 2 GiB | 16 GiB |
| **GPU** | ❌ 없음 | ✅ NVIDIA T4 (16GB VRAM) |
| **로컬 LLM 추론** | ❌ 불가 | ✅ 7B~13B 모델 가능 |
| **Playwright headless** | ✅ 가능 | ✅ 가능 |
| **EBS 용량** | 25 GiB | 50 GiB |
| **인스턴스 스토리지** | — | 125GB NVMe SSD (임시) |

> [!IMPORTANT]
> **시나리오 선택 가이드**
> - **시나리오 A 추천**: 외부 LLM API(GPT-4o, Gemini, Claude)를 사용하며, 비용 효율을 중시하는 경우
> - **시나리오 B 추천**: 외부 API 의존 없이 자체 모델로 추론하거나, API 비용을 줄이면서 대량 테스트가 필요한 경우
>
> 단, 시나리오 B는 **EC2 비용만으로 월 ₩30만 이상**이므로, 외부 LLM API 비용과 비교 후 결정하세요.

---

## 아키텍처 (공통)

```
┌──────────────────────────────────────────────────┐
│                  AWS Cloud (ap-northeast-2)       │
│                                                  │
│   ┌──────────── VPC (기본) ────────────────┐     │
│   │                                        │     │
│   │   ┌──────────────────────────────┐     │     │
│   │   │  EC2 (Windows Server)        │     │     │
│   │   │  ┌────────────────────────┐  │     │     │
│   │   │  │ FastAPI Backend        │  │     │     │
│   │   │  │ - 온톨로지             │  │     │
│   │   │  │ - 서브에이전트         │  │     │     │
│   │   │  │ - 하네싱 로직          │  │     │     │
│   │   │  └────────────────────────┘  │     │     │
│   │   │  ┌────────────────────────┐  │     │     │
│   │   │  │ Playwright (headless)  │  │     │     │
│   │   │  │ - 브라우저 자동화 검증  │  │     │     │
│   │   │  └────────────────────────┘  │     │     │
│   │   │  ┌────────────────────────┐  │     │     │
│   │   │  │ [B only] 로컬 LLM     │  │     │     │
│   │   │  │ - NVIDIA T4 GPU 추론   │  │     │     │
│   │   │  └────────────────────────┘  │     │     │
│   │   │                              │     │     │
│   │   │  EBS: gp3 (25~50 GiB)       │     │     │
│   │   └──────────────────────────────┘     │     │
│   │              │                         │     │
│   │   ┌──────────▼──────────────────┐      │     │
│   │   │  보안 그룹                    │      │     │
│   │   │  - RDP(3389), 80, 443, 8000 │      │     │
│   │   └─────────────────────────────┘      │     │
│   │              │                         │     │
│   │   ┌──────────▼──────────────────┐      │     │
│   │   │  탄력적 IP (Elastic IP)       │      │     │
│   │   └─────────────────────────────┘      │     │
│   └────────────────────────────────────────┘     │
└──────────────────────────────────────────────────┘
            │
            ▼
     사용자 입력 → 온톨로지 → 서브에이전트 → 하네싱 → 응답
```

---

### ⚠️ 산정에서 제외된 항목

| 항목 | 사유 |
|------|------|
| 외부 LLM API 비용 (OpenAI, Google 등) | AWS 비용이 아닌 별도 과금 |
| Windows Server 라이선스 | EC2 Windows 시간당 요금에 **이미 포함** |
| Route 53 (도메인/DNS) | 개발 단계에서는 IP 직접 접속 가정 |
| SSL 인증서 (ACM) | 개발 단계에서 HTTP 사용 또는 Let's Encrypt 사용 가정 |
| NAT 게이트웨이 | 퍼블릭 서브넷에 직접 배치하므로 불필요 |
| CloudWatch (모니터링) | 기본 모니터링 무료 |
